import { StyleSheet } from 'react-native';

const tutorial = StyleSheet.create({
	container: {
		flex: 1,
	},
	view: {
		flex: 1,
	},
	image: {
		resizeMode: 'contain',
	},
});

export default tutorial;
